-- Provides an interactive scrollbar.
-- Highlights are applied by theme.integrations.scrollview (re-run on theme/bg
-- change), so this spec stays a pure plugin declaration with no side effects.

return { "dstein64/nvim-scrollview" }
